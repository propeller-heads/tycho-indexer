# abigen output is regenerated by build.rs on every build
src/abi/*.rs
!src/abi/mod.rs
# `substreams protogen` writes its hash cache next to the manifest rather than to
# --output-path, so it lands here instead of in src/pb.
chains/src/
